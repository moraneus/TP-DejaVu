initiate
    MaxSpeed: int := 0
on recorded(maker: str, speed: int)
    MaxSpeed: int := ite(@MaxSpeed < speed, speed, @MaxSpeed)
    NewRecord: bool := @MaxSpeed < speed
output fast(maker, NewRecord)


